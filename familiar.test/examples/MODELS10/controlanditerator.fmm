varset = gc1.* // it can be written: varset = features gc1
foreach (f in varset) do
  newName = str_concat "new_" f
  rename_feature f as newName
end

/*
\begin{Verbatim}[commandchars=@\[\],numbers=left,firstnumber=1,stepnumber=1]
varset = gc1.* // it can be written: varset = features gc1
foreach f in varset do
  newName = concat new\_ f
  rename f as newName
end
\end{Verbatim}
*/