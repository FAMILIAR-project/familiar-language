fm1 = FM (A : [B] C D; B : (E|F|G) ; )

c1 = configuration fm2 // fm2 instead of fm1?

bc1 = isNull c1
assert (bc1)

fpA = parent A // root has no parent

bfpa = isNull fpA
assert (bfpa)

opA = operator A  // root has no operator

bopA = isNull opA
assert (bopA)

ftZ = name fm1.Z // fm1.Z does not exist!

bftZ = isNull ftZ
assert (bftZ)

exit

