run "GraphicalCard.fmm" into GC
shortMSPL = { GC.GraphicalCardn2 GraphicalCardn3 GraphicalCardn8 }
nGC = card GC.*
vgc2 = valid? GC.GraphicalCardn2
nine = 9
ten = 10
if (vgc2) then printinfo nGC end  
if (nGC < nine) then printinfo nGC end
if (nGC eq ten) then printinfo nGC end 

//fm = if (truc) then merge union (f*) else merge intersection (f*...) end

//if (truc) then
// fm = merge union (f*) // shortMSPL GC.*
//else
//  fm = merge intersection (f*...) end
