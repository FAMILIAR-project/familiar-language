conf2 = copy conf1 // conf2 eq conf1 but not (conf1 == conf2)

nb = counting gc1 // number of valid configurations: 8

b1 = isValid conf1
b2 = (selectedF conf1) eq (selectedF conf2) // true

b2 = compare gc1 gc2 // refactoring

...