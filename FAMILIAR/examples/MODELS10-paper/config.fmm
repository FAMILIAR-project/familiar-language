conf1 = configuration gc1 // create a configuration of gc1

b1 = select Multi in conf1 // feature Multi of gc1 is selected

b2 = deselect Multi in conf1 // override the previous selection

b3 = unselect Multi in conf1 // Multi is neither selected nor deselected

...