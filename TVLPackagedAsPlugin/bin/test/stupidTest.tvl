enum color in { Red, Blue, White };
root Car {
	color mainColor;
	enum optionnalColor in { Red, Blue, White };
	mainColor == optionnalColor;
	//mainColor == optionnalColor;
	//mainColor == Blue;
	/*((((( this.optionnalColor1 ))))) == ( Red );
	( Ferrari.optionnalColor2 ) == ( Red );
	( Ferrari.optionnalColor2 ) == ( Red );*/
	group oneof {
		Ford, 
		Ferrari {
			//parent.mainColor == parent.optionnalColor1;
			//enum optionnalColor2 in { Red, Blue };
			//optionnalColor2 == Blue;
			
		}
	}
	//this.optionnalColor in { Red };
	//Car.Ford excludes Ferrari;
	//this.mainColor >= this.optionnalColor;
	//this.mainColor <= this.optionnalColor;
	//this.mainColor > this.optionnalColor;
	//this.mainColor < this.optionnalColor;
	//Red >= this.optionnalColor;
	//Red <= this.optionnalColor;
	//Red > this.optionnalColor;
	//Red < this.optionnalColor;
	//this.mainColor >= Blue;
	//this.mainColor <= Blue;
	//this.mainColor > Blue;
	//this.mainColor < Blue;
}