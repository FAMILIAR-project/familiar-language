root Car {
	bool type;
	bool sport;
	bool family;
	group oneof {
		Ferrari {
			//ifin: parent.sport == true;
			//ifin: parent.family == false;
			//Ferrari || ( true || ( false ) );
			//parent.type == or( or(true, false, true), true );
			parent.type == or( parent.family, parent.sport );
		},
		Ford {
			//ifin: parent.sport == false;
			//ifin: parent.family == true;
		}
	}
}