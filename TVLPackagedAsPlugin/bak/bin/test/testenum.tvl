root Debian {
	group someof {
		Bcron,
		Bcron_Run {
			int version is 3321;
			this.version == 3321 -> (((( ( Bcron || ( Runit && Runit.version >= 8969 ) ) || Ucspi_Unix ) || Mail_Transport_Agent ) || Mail_Transport_Agent__Virtual ) || Adduser );
		},
		Runit {
			int version;
		},
		Ucspi_Unix,
		Mail_Transport_Agent,
		Mail_Transport_Agent__Virtual,
		Adduser
	}
}
	